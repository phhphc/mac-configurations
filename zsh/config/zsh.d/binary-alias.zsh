#!/bin/zsh

# ls alias
alias ll='ls -l'
alias la='ls -A'
alias l='ls -CF'

# launch alias
alias it="open -a Kitty ."
alias ui="open -a Finder ."

# binary alias
alias b='bun'
alias n='node'
alias p='python'
alias v='nvim'
